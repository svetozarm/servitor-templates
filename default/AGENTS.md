You are working in a servitor app. Servitor is a single-binary server that serves a static frontend and proxies API requests to a Go CGI backend, with automatic git-based data sync. The backend is invoked as a subprocess that reads raw HTTP requests from stdin.

Read `SKILL.md` for the full API reference and instructions on how to interact with the app.
